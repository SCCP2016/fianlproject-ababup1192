# -*- coding: ascii-8bit -*-
class CommandParser
  attr_reader :argv
  def initialize(argv)
    @argv = argv
  end

  # コードを追加
  def parse
    options = @argv.drop(2)
    options_hash = Hash[*options]
    Command.new(@argv[0], @argv[1], "Ex", 2, 13)
  end
end
